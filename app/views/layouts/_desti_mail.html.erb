 
<section class="bg-light">

      <div class="row justify-content-center">
         <div class="container gallery">
        <div class="row justify-content-center">
          <div class="col-md-6 text-center">
            <h2><b>Discover</b> our cities.</h2>
            <p>Select the city of your dreams and discover all the miffers who live there.</p>
          </div>
        </div>
        <ul class="masonry row gutter-2">
              <% @cities.each do |city|%>
              <li class="col-md-6 col-lg-3">
                <% if city.name == "Paris" %>
                  <article class="tile">
                <% end %>
                <% if city.id.even? %>
                  <article class="tile tile-long">
                <% else %>
                  <% if city.id % 5 == 0 %>
                    <article class="tile tile-long">
                  <% end %>
                  <article class="tile">
                <% end %>
                  <div class="tile-image" style="background-image: url('<%=asset_path("cities/#{city.name.downcase}.jpg")%>');"></div>
                    <%= link_to city_path(city) do%>
                    <div class="tile-header">
                      <h2 class="text-white" style="text-align: center"><%= city.name %></h2>
                      <h4 class="text-white" style="text-align: center"><strong><%= city.users.count %> miffers </strong></h4>
                    </div>
                    <% end %>
                </article>
              </li>
              <% end %>
            </ul>
            <div class="row">
          <div class="col text-center">
            <button type="button" class="btn btn-rounded btn-with-ico btn-primary" href="https://mif-project.herokuapp.com/">View All Cities <i class="icon-chevron-right2 fs-24"></i></button>
          </div>
        </div>
          </div>
        </div>
      </div>
    </section>


 <!-- video -->
    <section class="bg-primary separator-bottom">
      <div class="container">
        <div class="row justify-content-between align-items-center">
          <div class="col-md-5 pl-md-5 text-center text-md-left text-white">
            <span class="eyebrow text-orange mb-1">Special for you</span>
            <h2 class="mb-2"><b>Air Baloon</b> Festival</h2>
            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Eos tempore, inventore accusamus odio</p>
            <a href="https://mif-project.herokuapp.com/" class="btn btn-primary btn-rounded btn-blue">Discover Now</a>
          </div>
          <div class="col-md-7" data-aos="zoom-in">
           <img src="../assets/cities/prague.jpg" alt="Image">
            </div>
        </div>
      </div>
    </section>
    <!-- / video -->