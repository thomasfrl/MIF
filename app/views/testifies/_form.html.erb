<%= form_with(model: testify, local: true) do |form| %>
  <% if testify.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testify.errors.count, "error") %> prohibited this testify from being saved:</h2>

      <ul>
      <% testify.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :accepted %>
    <%= form.check_box :accepted %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
