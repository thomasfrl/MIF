<%= form_with(model: correspondance, local: true) do |form| %>
  <% if correspondance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(correspondance.errors.count, "error") %> prohibited this correspondance from being saved:</h2>

      <ul>
      <% correspondance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user1_id %>
    <%= form.text_field :user1_id %>
  </div>

  <div class="field">
    <%= form.label :user2_id %>
    <%= form.text_field :user2_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
